
<section class="main-page">
<h1 class="main-title">JUNE 2024</h1>

<table class="budget-progress">
  
  <tr>
    <td><strong>Categories</strong></td>
    <td><strong></strong></td>
    <td><strong></strong></td>
  </tr>

  <% @budget_categories.each do |category| %>
    <%= render 'category', name: category.category.name, budget: category[:budget_amount]/100, fact: category[:fact_amount]/100, precentage: (category[:budget_amount]/category[:fact_amount])*100 %>
  <% end %> 

</table>

</section>